# ==================================================
# Company Data Enrichment System - Requirements
# ==================================================

# --- Core Data Processing ---
pandas>=2.1.0
numpy>=1.26.0

# --- HTTP & Scraping ---
requests>=2.31.0
httpx>=0.27.0               # Async-capable HTTP client
beautifulsoup4>=4.12.3      # HTML parsing
# lxml>=5.2.0                 # Fast XML/HTML parser backend for BS4
urllib3>=2.2.0              # HTTP library (used by requests)
fake-useragent>=1.5.1       # Rotate user agents to avoid blocks

# --- GenAI / LLM ---
openai>=1.30.0              # OpenAI API client (GPT-4o, etc.)
tiktoken>=0.7.0             # Token counting for OpenAI models

# --- Structured Output / Validation ---
pydantic>=2.7.0             # Data validation and structured output
pydantic-settings>=2.3.0    # Settings management via .env

# --- Environment & Configuration ---
python-dotenv>=1.0.1        # Load .env files into os.environ

# --- Async & Concurrency ---
aiohttp>=3.9.5              # Async HTTP requests (optional enrichment path)
aiofiles>=23.2.1            # Async file I/O

# --- Caching ---
cachetools>=5.3.3           # In-memory LRU/TTL caches
diskcache>=5.6.3            # Persistent disk-based cache for domain lookups

# --- Rate Limiting & Retries ---
tenacity>=8.3.0             # Retry with exponential backoff
ratelimit>=2.2.1            # Simple rate limiting decorator

# --- URL Utilities ---
tldextract>=5.1.2           # Extract domain components (subdomain, domain, TLD)
validators>=0.28.3          # URL and email validation

# --- Logging & CLI ---
rich>=13.7.1                # Beautiful terminal output and logging
click>=8.1.7                # CLI framework for the interactive menu

# --- Fuzzy Matching (deduplication) ---
rapidfuzz>=3.9.0            # Fast fuzzy string matching for company name dedup
python-Levenshtein>=0.25.1  # Speed boost for Levenshtein distance

# --- Testing ---
pytest>=8.2.0
pytest-asyncio>=0.23.7
pytest-cov>=5.0.0
responses>=0.25.3           # Mock HTTP responses in tests

# --- Type Stubs (optional, for IDE support) ---
types-requests>=2.31.0
types-beautifulsoup4>=4.12.0
